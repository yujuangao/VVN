% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_vvn.R
\name{theme_vvn_research}
\alias{theme_vvn_research}
\title{VVN Research Hub theme}
\usage{
theme_vvn_research(base_size = 12, base_family = "sans", grid = "y")
}
\arguments{
\item{base_size}{Base font size. Default \code{12}.}

\item{base_family}{Font family. Default \code{"sans"}.}

\item{grid}{One of \code{"y"} (horizontal only), \code{"x"}, \code{"both"}, \code{"none"}.}
}
\value{
A \code{ggplot2::theme} object.
}
\description{
A clean, academic publication-quality ggplot2 theme inspired by the design
of research data-blog sites (e.g., the DHS Research Hub at
\url{https://tech.popdata.org/dhs-research-hub/}).

Key differences from \code{theme_vvn()}:
\itemize{
  \item Near-black (\code{#1A1A1A}) titles instead of VT Maroon —
        neutral / publication-ready.
  \item No axis lines or ticks — pure grid-only minimal look.
  \item Legend defaults to \strong{bottom / horizontal} for easier reading.
  \item Left-aligned caption (matching blog conventions).
  \item Neutral gray facet strips (not VT Maroon).
  \item Grid lines are slightly lighter (\code{#EBEBEB}) for a softer feel.
}

Pairs naturally with the \code{"forest"} and \code{"forest_div"} palettes for
environmental / land-cover research visualizations.
}
\examples{
library(ggplot2)
ggplot(ggplot2::mpg, aes(displ, hwy, colour = class)) +
  geom_point() +
  theme_vvn_research()
}
